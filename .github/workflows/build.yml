name: Build Android .so (Manual)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Clone repository manually
        run: |
          git clone https://github.com/${{ github.repository }}.git repo
          cd repo
          git checkout ${{ github.sha }}

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y cmake build-essential

      - name: Clone Geode SDK
        run: |
          git clone --depth 1 https://github.com/geode-sdk/geode-sdk.git
          echo "GEODE_SDK=$(pwd)/geode-sdk" >> $GITHUB_ENV

      - name: Build
        run: |
          cd repo
          mkdir build && cd build
          cmake .. -DCMAKE_BUILD_TYPE=Release -DGEODE_TARGET=Android64 -DGEODE_SDK=${{ env.GEODE_SDK }}
          make -j$(nproc)

      - name: Upload .so
        uses: actions/upload-artifact@v4
        with:
          name: geode-mod-android
          path: repo/build/*.so
